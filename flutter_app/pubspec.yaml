name: mrrss_flutter
description: Cross-platform Flutter client for MrRSS.
publish_to: none
version: 1.3.23+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
